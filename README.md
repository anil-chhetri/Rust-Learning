# Rust-Learning

## getting started with Rust.

command to create a new project 

```rust
cargo new exercise
```

command to run the project. 

```rust
cd exercise && cargo run
```

also note you can run `cargo check` to quickly check your project for erros, use `cargo build` to compile it wihout running it.